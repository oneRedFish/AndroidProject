<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="android:Theme.Material.Light.DarkActionBar"></style>
    <!-- Customize your theme here. -->
    <style name="Toolbar">
        <item name="android:background" parent="Theme.AppCompat.Light.NoActionBar">
            @color/colorYellow
        </item>
    </style>

    <style name="PopToolbar" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="overlapAnchor">false</item>
        <item name="android:dropDownVerticalOffset">-5dp</item>
    </style>

</resources>
